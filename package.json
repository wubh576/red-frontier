{
  "name": "red-frontier",
  "version": "0.1.0",
  "private": true,
  "description": "An offline single-player browser RTS",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check engine.js && node --check game.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
